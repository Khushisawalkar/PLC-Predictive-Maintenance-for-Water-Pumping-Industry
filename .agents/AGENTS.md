### Rule: Revert Changes
When the user explicitly asks to "revert the changes" or "undo", you MUST immediately prioritize reverting the relevant files or undoing the recent actions to their previous state, before proceeding with any other tasks.
